import styled from 'styled-components';

export const CarouselArrowImg = styled.img`
  width: 100%;
  height: auto;
  object-fit: contain;
`;

export const Button = styled.div`
  cursor: pointer;
  align-self: center;
  justify-self: center;
`;
